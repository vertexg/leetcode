class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
      total = 0:
      for i range nums:
        for b range nums:
          total += nums[i] + nums[b]
          if target = total:
            return [i,b]j]
